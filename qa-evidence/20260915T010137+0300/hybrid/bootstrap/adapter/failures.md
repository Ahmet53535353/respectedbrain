# Failures

No unresolved hybrid bootstrap lifecycle failure. An invalid negative CLI flag correctly failed before target creation. One sandbox hash attempt produced null values and was discarded; a separate fail-fast hash verification passed.
