# Result

Status: VERIFIED

Published Windows `main` launchers were run against a disposable NTFS vault with `-Environment hybrid`. Config rendered as `windows-wsl`/`hybrid`. Install, two same-version updates, and two data-preserving uninstalls returned exit 0; the vault remained and all observed staging clones were cleaned. Cross-boundary WSL daily writing is covered separately by the physical hybrid smoke evidence.
