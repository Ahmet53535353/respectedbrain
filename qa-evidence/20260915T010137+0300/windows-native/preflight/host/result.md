# Preflight Result

Status: **FAILED**

The host, repository, WSL, filesystem, agents, Obsidian, scheduler, MCP names, live-vault path safety, and two backup forms were inventoried. Backup verification is complete. Preflight remains `FAILED` because installed managed hooks point to an absent `py.exe`; later lifecycle tests must reproduce and fix or explicitly classify this installation defect.
