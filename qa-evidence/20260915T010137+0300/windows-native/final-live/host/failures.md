# Failures

The initial final-state check found two old managed briefing tasks. Root cause was the uninstaller's obsolete-name/localized-LIST parser. The new RED/GREEN CSV prefix regression passed and both exact managed tasks were removed; final count is zero.
