# Residual Risks

- No authenticated three-turn provider test completed. Codex requires explicit approval for the signed-in-account/hook-trust invocation; Claude, Cursor, Antigravity, and Gemini CLIs were unavailable on tested hosts.
- No true Linux VM or physical macOS was available. WSL and CI are not substitutes.
- AI-native BOOTSTRAP installation and repeat behavior passed on Windows, pure WSL, and hybrid. A real autonomous third-party coding-agent interpretation of `BOOTSTRAP.md` is still part of the blocked authenticated-provider scope.
- Hybrid bootstrap, published one-liner, and local-CLI option matrices passed, but simultaneous real Windows/WSL authenticated-provider writes remain blocked by account/tool availability.
- The final Python path is a per-user Python 3.14.7 installation outside PATH. It is executable and rendered as an absolute path, but uninstalling that Python later will break hooks.
- The Dashboard comparison relies on managed-block boundaries: one archived/live line changed inside the managed briefing block. Human-authored regions were unchanged.
- `origin/main` contains all eleven fixes through tested product commit `b835c19`. No force-push was used.
